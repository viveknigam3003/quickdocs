"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveWebApiPath = void 0;
const webApi = require("../data/web-api.json");
function resolvePath(root, names, i = 0) {
    if (!root)
        return;
    const api = root[names[i].path];
    if (typeof api === "string") {
        if (i === names.length - 1) {
            return api;
        }
        return undefined;
    }
    else if (typeof api === "object") {
        if (i === names.length - 1) {
            return api.url;
        }
        switch (names[i + 1].navigation) {
            case "#":
            case "~":
                return resolvePath(api.inst, names, i + 1);
            case ".":
                return (resolvePath(api.stat, names, i + 1) ||
                    resolvePath(api.inst, names, i + 1));
        }
    }
    return;
}
function resolveWebApiPath(names) {
    return resolvePath(webApi, names);
}
exports.resolveWebApiPath = resolveWebApiPath;
